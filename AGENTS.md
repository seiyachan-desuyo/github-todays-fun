# 项目开发指南

这是一个标准 Next.js App Router + TypeScript + Tailwind CSS 项目。

- 包管理器：pnpm；Node.js 20+
- 页面：`src/app/`
- 业务组件：`src/components/`
- 数据管道：`src/lib/`
- 静态刊物：`src/data/editions/`
- 修改后运行：`pnpm lint && pnpm test && pnpm build`
- 不得把上游空响应解释为“没有项目”；必须保留来源状态和原始响应。
- AI 文案只能基于真实 description/README；缺少密钥时不得根据项目名生成内容。
