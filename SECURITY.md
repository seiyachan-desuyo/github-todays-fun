# 安全政策

## 支持范围

当前维护版本为最新的默认分支。旧版本不承诺持续提供安全修复。

## 报告安全问题

请不要在公开 Issue 中提交 Token、App Secret、用户 ID、未脱敏日志或可利用细节。请通过 GitHub 仓库维护者主页提供的私密联系方式报告，并附：

- 受影响版本或提交；
- 最小复现步骤；
- 影响范围；
- 已做的脱敏处理。

维护者确认问题后会协调修复与披露时间。

## 凭证原则

项目不要求把任何密钥提交到仓库。`GITHUB_TOKEN`、`LARK_APP_SECRET`、`VERCEL_TOKEN` 等只应存放在本地环境变量或托管平台的 Secret 管理中。
